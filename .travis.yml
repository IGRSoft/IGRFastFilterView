language: objective-c
osx_image: xcode8
xcode_workspace: IGRFastFilterView.xcworkspace
xcode_scheme: IGRFastFilterView
before_install:
    - export LANG=en_US.UTF-8
    - brew update
    - brew outdated xctool || brew upgrade xctool
    - gem install cocoapods
    - pod install